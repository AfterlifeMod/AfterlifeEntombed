# Death's Bargain passive - grant armor based on missing health
# This is called from the Origins power and applies dynamic armor scaling

# The attribute modifier will be applied via the Java event handler
# This function exists to trigger the Java-side logic every tick
